pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/7sanjay/docker.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t my-app:latest .'
            }
        }
    }
}
